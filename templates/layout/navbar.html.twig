<nav class="navbar navbar-expand-lg navbar-light bg-main pl-4 pr-4 pt-1 pb-1">
      <a class="navbar-brand brand-logo" href="{{ path('index') }}" title="InConnect">
          InConnect
      </a>
      <button style="border:none;" class="navbar-toggler" data-toggle="collapse" data-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
         <i style="font-size:34px;color:white;" class="fas fa-arrow-circle-down"></i>
     </button>
     <div class="collapse navbar-collapse" id="navbarContent">
         {% if app.user is not null %}
            <ul class="navbar-nav mr-auto">
                <li class="dropdown nav-item ml-1 mr-4">
                     <a class="dropdown-toggle nav-link" data-toggle="dropdown" href="#">
                         <img id="actual-user-profile-pic" title="{{ app.user.firstName ~ ' ' ~ app.user.lastName }}" class="xs-profile" src="{{ asset('images/profiles/' ~ app.user.profilePic) }}" alt="Navigation bar profile picture">
                     </a>
                     <ul class="dropdown-menu">
                         <li><a class="nav-link" href="{{ path('view_user', { permalink: app.user.permalink }) }}"><i class="fas fa-user"></i> Profile</a></li>
                         <li><a class="nav-link" href="{{ path('user_pages') }}"><i class="fas fa-file-alt"></i> Pages</a></li>
                         <li><a class="nav-link" href="{{ path('settings') }}"><i class="fas fa-cogs"></i> Settings</a></li>
                         <li><a class="nav-link" href="{{ path("logout") }}"><i class="fas fa-sign-out-alt"></i> Log out</a></li>
                     </ul>
                </li>

                {# Friend Notifications #}
                <li title="Friend notifications" class="dropdown nav-item ml-1">
                    <a id="notification-link" class="dropdown-toggle nav-link" data-toggle="dropdown"
                       href="{{ path("notification") }}">
                        <i class="fas fa-user-friends nav-icon"></i>
                        <span id="friend-notification-counter" class="badge badge-warning"></span>
                    </a>

                    <ul id="friend-notification-list" style="list-style:none;" class="dropdown-menu mr-5"></ul>
                </li>

                {# Messages #}
                <li title="Messages" class="dropdown nav-item ml-1">
                    <a class="dropdown-toggle nav-link" data-toggle="dropdown" href="#">
                        <i class="fas fa-envelope nav-icon"></i>
                        <span id="message-notification-counter" class="badge badge-warning"></span>
                    </a>
                    <ul id="message-list" class="dropdown-menu mr-5"></ul>
                </li>

                {# Other Notifications #}
                <li title="Notifications" class="dropdown nav-item ml-1">
                    <a class="dropdown-toggle nav-link" data-toggle="dropdown" href="#">
                        <i class="fas fa-bell nav-icon"></i>
                        <span id="general-notification-counter" class="badge badge-warning"></span>
                    </a>
                    <ul id="notification-list" class="dropdown-menu mr-5"></ul>
                </li>

            </ul>
            <ul class="navbar-nav mx-auto">
                <li class="nav-item nav-link"><a href="{{ path('index') }}">News</a></li>
                <li class="nav-item nav-link"><a href="{{ path('messages') }}">Messages</a></li>
                <li class="nav-item nav-link"><a href="{{ path('events') }}">Events</a></li>
                <li class="nav-item nav-link"><a href="{{ path('find_friends') }}">Find Friends</a></li>
            </ul>
            <ul class="navbar-nav mr-auto">
                <li>
                    <form class="form-inline input-group" method="POST">
                        <input id="search-field" href="{{ path('search') }}" class="form-control" type="search" placeholder="Search" aria-label="Search" autocomplete="off">
                    </form>
                    <ul id="search-list" class="mr-5"></ul>
                </li>
                <li class="navbar-brand" style="display: none;"><a id="actual-user-name" class="brand-logo" href="{{ asset('u/' ~ app.user.permalink) }}">{{ app.user.firstName ~' '~ app.user.lastName}}</a></li>
            </ul>
         {% else %}
             <ul class="navbar-nav mr-auto">
                <li class="nav-item nav-link"><a href="{{ path("signup") }}">Sign up</a></li>
                <li class="nav-item nav-link"><a href="{{ path("login") }}">Log in</a></li>
             </ul>
         {% endif %}
     </div>
</nav>